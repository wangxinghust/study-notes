#include <iostream>
#include <string>
#include <stack>
using namespace std;

stack<char> ch;



int main()
{
	string str;
	getline(cin, str);
	for (int i = 0; i < str.size(); ++i)
	{
		if (str[i] <= '9' && str[i] >= 0)
		{

		}
	}
}